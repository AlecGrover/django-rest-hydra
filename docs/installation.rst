============
Installation
============

At the command line::

    $ easy_install django-rest-hydra

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-rest-hydra
    $ pip install django-rest-hydra
